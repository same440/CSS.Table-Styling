/* 🌐 CSS Table Styling | টেবিলের ডিজাইন সেটআপ 🌐 */

table, tr, td {
  border: 1px solid;                 /* 🧱 সাধারণ এক পিক্সেল পুরু বর্ডার সব টেবিল, সারি, ও সেল এ */
  border-collapse: collapse;        /* 📏 বর্ডারগুলো একত্রে মিশে যাবে (ডাবল লাইনের বদলে সিংগেল) */
  padding: 5px 10px;                /* 🔲 উপরে-নিচে ৫px, ডানে-বামে ১০px স্পেস দিচ্ছে */
  text-align: center;               /* 🔤 টেক্সট সেন্টারে থাকবে */
}

table {
  width: 100%;                      /* 📏 টেবিল সম্পূর্ণ পেজ বা কনটেইনার জুড়ে থাকবে */
}

th {
  height: 50px;                     /* 📏 শিরোনাম (header cell) গুলোর উচ্চতা ৫০px */
  text-align: left;                 /* ❌ প্রথমে বাম দিকে ঠিক করা ছিলো */
  text-align: center;              /* ✅ কিন্তু পরে সেন্টারে সেট করা হয়েছে (এইটাই কাজ করবে) */
  background: yellow;              /* 🟡 হেডারের ব্যাকগ্রাউন্ড হলুদ */
  color: black;                    /* ⚫ হেডারের লেখা কালো */
  border: 1px solid red;           /* 🔴 হেডারে লাল বর্ডার */
}

td {
  background: pink;                /* 🌸 টেবিল ডেটা সেলের ব্যাকগ্রাউন্ড পিংক */
  color: black;                    /* ⚫ টেক্সট কালো */
  border: 1px solid green;         /* 🟩 সব সেলের বর্ডার সবুজ */
  height: 35px;                    /* 📏 প্রতিটি সেলের উচ্চতা ৩৫px */
}

tr:hover th, tr:hover td {
  background: blue;                /* 🔵 হোভার করলে (মাউস নিলে) পুরো সারির ব্যাকগ্রাউন্ড নীল হবে */
  color: white;                    /* ⚪ লেখা সাদা হবে */
}

div {
  width: 400px;                    /* 📏 div কন্টেইনারের প্রস্থ ৪০০px */
  overflow-x: auto;                /* 📜 যদি div এর ভিতরের কনটেন্ট বেশি হয়, তাহলে scroll আসবে */
}
